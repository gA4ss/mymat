template <class T>
Vector<T> sub(const Vector<T>& v1, const Vector<T>& v2) {
  Vector<T> res;
  return res;
}

template <class T>
Vector<T> sub(const Vector<T>& v, const T& c) {
  Vector<T> res;
  return res;
}